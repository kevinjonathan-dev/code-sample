const USER = {
  SET: 'set-user',
  SET_ACCESS_TOKEN: 'set-access-token',
  SET_IS_MANAGER: 'set-is-manager',
  SET_IS_USER: 'set-is-user',
  SET_IS_ADMIN: 'set-is-admin',
};

const RESET = 'reset-store';

const ACTIONS = {
  USER,
  RESET,
};

export default ACTIONS;
